if (WIN32)
    add_subdirectory(gina)
    add_subdirectory(credential-provider)
else (WIN32)
#     add_library(pam_flexvdi_cred SHARED cred_channel.c pam_ovirt_cred.c)
#     target_link_libraries(pam_flexvdi_cred pam)
endif(WIN32)
